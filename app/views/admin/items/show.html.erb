<h2>商品詳細</h2>
<div>
  <p><%= image_tag @item.get_profile_image(100,100) %></p>
  <p>商品名：<%= @item.name %></p>
  <p>税込価格(税抜価格）：<%= @item.price %></p>
  <p><%= link_to '編集する', edit_admin_item_path(@item.id), class: "btn btn-success" %></p>
</div>
